nginx配置场景　

代理服务，动态缓存，动静分离，负载均衡，nginx与lua的开发

对sql的注入防攻击，对请求的访问控制，对请求的频率控制，对防爬虫

http性能压测，性能瓶颈分析，系统性能优化，基于Nginx的性能配置优化

基础，场景实战，深度学习，架构

安装

确认系统网络，确认关闭iptables规则，确认停用selinux

查看
sudo iptables -L 
iptables -t nat -L

关闭
sudo iptables -F
iptables -t nat -F

getenforce
serenforce 0
